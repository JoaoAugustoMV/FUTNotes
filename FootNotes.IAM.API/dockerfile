# Stage 1: Build the application
FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
WORKDIR /src

COPY ["FootNotes.IAM.API/FootNotes.IAM.API.csproj", "FootNotes.IAM.API/"]
RUN dotnet restore "FootNotes.IAM.API/FootNotes.IAM.API.csproj"
COPY . .

WORKDIR /src/FootNotes.IAM.API
RUN dotnet build "FootNotes.IAM.API.csproj" -c Release -o /app/build

# Stage 2: Publish the application
FROM build AS publish
RUN dotnet publish "FootNotes.IAM.API.csproj" -c Release -o /app/publish /p:UseAppHost=false

# Stage 3: Create the final runtime image
FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS final
WORKDIR /app
EXPOSE 80
COPY --from=publish /app/publish .
ENTRYPOINT ["dotnet", "FootNotes.IAM.API.dll"]